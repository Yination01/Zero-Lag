// Config plugin placeholder. The native module is picked up by Expo's
// autolinking once plugins/android sources are included by prebuild. Keep
// this file so app.json can reference the plugin folder if needed.
module.exports = function withZeroLagNet(config) {
  return config;
};
